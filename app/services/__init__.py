# Ingestion pipeline, retrieval pipeline, generation, verification.
# Populated sprint-by-sprint starting Sprint 1 (see project task list) —
# each concrete class here implements an interface from app/core/interfaces.py.
