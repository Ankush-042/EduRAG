# NOTE: audio extraction also needs `ffmpeg` on PATH (system install, not
# pip-installable) — e.g. `winget install ffmpeg` / `choco install ffmpeg`.
#
# NOTE: for GPU transcription (optional — CPU always works as a fallback),
# also install the full CUDA-12 runtime set (see app/services/
# transcription.py::_NVIDIA_PIP_PACKAGES, which is the source of truth —
# keep this comment in sync with it):
#   nvidia-cublas-cu12 nvidia-cudnn-cu12 nvidia-nvjitlink-cu12
#   nvidia-cusparse-cu12 nvidia-curand-cu12 nvidia-cuda-runtime-cu12
#   nvidia-cuda-nvrtc-cu12
# cuBLAS/cuDNN are what ctranslate2 calls directly; the rest are THEIR
# transitive dependencies on CUDA 12 (nvJitLink in particular — without
# it, cublas64_12.dll fails to resolve its own dependency, which
# ctranslate2 reports as "cublas64_12.dll is not found or cannot be
# loaded" even though the file itself is present). This is the full pip
# runtime set, not the 3GB+ CUDA Toolkit — it's still ~1GB total but
# far smaller and inference-only. Not listed below as hard requirements
# since it's pointless on a CPU-only/non-NVIDIA machine.
streamlit>=1.38
pydantic>=2.7
pydantic-settings>=2.4
sqlalchemy>=2.0
alembic>=1.13
qdrant-client>=1.10
rank-bm25>=0.2.2
sentence-transformers>=3.0
faster-whisper>=1.0
yt-dlp>=2024.8.6
ffmpeg-python>=0.2.0
groq>=0.9
python-dotenv>=1.0
tenacity>=8.5
