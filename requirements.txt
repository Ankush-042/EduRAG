# NOTE: audio extraction also needs `ffmpeg` on PATH (system install, not
# pip-installable) — e.g. `winget install ffmpeg` / `choco install ffmpeg`.
#
# NOTE: for GPU transcription (optional — CPU always works as a fallback),
# also install: nvidia-cublas-cu12 nvidia-cudnn-cu12
# These ship just the cuBLAS/cuDNN runtime DLLs ctranslate2 needs, without
# requiring the full CUDA Toolkit. Not listed below as hard requirements
# since they're large (100s of MB) and pointless on a CPU-only/non-NVIDIA
# machine.
streamlit>=1.38
pydantic>=2.7
pydantic-settings>=2.4
sqlalchemy>=2.0
alembic>=1.13
qdrant-client>=1.10
rank-bm25>=0.2.2
sentence-transformers>=3.0
faster-whisper>=1.0
yt-dlp>=2024.8.6
ffmpeg-python>=0.2.0
groq>=0.9
python-dotenv>=1.0
tenacity>=8.5
